import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sfeir.org',
  appName: 'Automation',
  webDir: 'www',
  // plugins: {
  //   CapacitorHttp: {
  //     enabled: true,
  //   },
  // },
};

export default config;
